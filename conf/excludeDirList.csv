# Enter here the directories to exclude, one per line
# this is a comment
#
#~snapshot
# to exclude all directories ending with ~snapshot using regex : ~snapshot$
# to exclude .git and .test directories with string match
.git
.test
# to exclude all directories starting with a dot using regex : ^\.
#^\.